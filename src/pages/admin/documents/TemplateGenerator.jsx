/**
 * Template generation module — routed at /admin/templates.
 * Shares the letters & templates authoring UI (documentation: Template Generation).
 */
export { default } from './LettersTemplates.jsx'
